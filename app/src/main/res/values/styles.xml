<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonAnswer" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/color_white</item>
        <item name="android:textSize">35sp</item>
        <item name="colorButtonNormal">@color/color_rta</item>
        <item name="colorControlHighlight">@color/color_white</item>
    </style>
    <style name="ButtonAnswer2" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/color_rta</item>
        <item name="android:textSize">35sp</item>
        <item name="colorButtonNormal">@color/color_white</item>
        <item name="colorControlHighlight">@color/color_rta</item>
    </style>

</resources>
